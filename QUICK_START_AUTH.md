# 🚀 Быстрый старт - Firebase Authentication

## ⚡ Что нужно сделать перед деплоем

### 1. Создайте KV Namespace

```bash
npx wrangler kv:namespace create PUBLIC_JWK_CACHE_KV
```

Вы получите ID вида:

```
{ binding = "PUBLIC_JWK_CACHE_KV", id = "1234567890abcdef" }
```

### 2. Обновите wrangler.jsonc

Замените `YOUR_KV_ID_HERE` на полученный ID:

```jsonc
"kv_namespaces": [
  {
    "binding": "PUBLIC_JWK_CACHE_KV",
    "id": "1234567890abcdef"  // ← Вставьте ваш ID сюда
  }
]
```

### 3. Настройте Firebase Console

1. Откройте [Firebase Console](https://console.firebase.google.com/project/planer-246d3/authentication)
2. Включите **Email/Password** в разделе Sign-in method
3. Добавьте домен `planer.m-k-mendykhan.workers.dev` в Authorized domains
4. Убедитесь, что в шаблоне письма для подтверждения используется правильный URL

### 4. Деплой

```bash
npm run deploy
```

## ✅ Что уже готово

- ✅ Регистрация с email/password
- ✅ Вход в систему
- ✅ Сброс пароля через email
- ✅ Обязательная проверка email
- ✅ Автоматическая отправка письма с подтверждением
- ✅ Редирект на `https://planer.m-k-mendykhan.workers.dev/` после подтверждения
- ✅ JWT токены в localStorage
- ✅ Защищенные API endpoints
- ✅ Красивый UI с Material-UI
- ✅ Проверка просроченных токенов
- ✅ Блокировка доступа без подтвержденного email

## 🔐 Как это работает

### Для пользователя:

1. **Регистрация** → Получает письмо с подтверждением
2. **Подтверждение email** → Кликает ссылку в письме
3. **Редирект** → Возвращается в приложение
4. **Вход** → Может войти только с подтвержденным email
5. **Доступ** → Получает полный доступ к приложению
6. **Сброс пароля** (при необходимости) → Кликает "Forgot password?" → Получает письмо → Устанавливает новый пароль

### На backend:

- Публичные endpoints: `/api/test`, `/api/versions` - доступны всем
- Защищенные endpoints: `/api/profile` и др. - требуют токен + подтвержденный email
- Middleware автоматически проверяет токены и `email_verified`

## 📝 Пример использования API

```typescript
import { useAuth } from './contexts/AuthContext';

function MyComponent() {
  const { getIdToken } = useAuth();
  
  const fetchData = async () => {
    const token = await getIdToken();
    
    const response = await fetch('/api/profile', {
      headers: {
        'Authorization': `Bearer ${token}`,
      },
    });
    
    const data = await response.json();
  };
}
```

Или используйте готовую утилиту:

```typescript
import { authenticatedFetch, getUserProfile } from './utils/api';

// Вариант 1: напрямую
const response = await authenticatedFetch('/api/profile');

// Вариант 2: через готовую функцию
const profile = await getUserProfile();
```

## 🧪 Тестирование

### Локальная разработка:

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

### После деплоя:

1. Откройте `https://planer.m-k-mendykhan.workers.dev/`
2. Зарегистрируйтесь с реальным email
3. Проверьте почту и подтвердите email
4. Войдите в систему

### Тест API:

```bash
# Публичный endpoint (без токена)
curl https://planer.m-k-mendykhan.workers.dev/api/test

# Защищенный endpoint (с токеном)
curl -H "Authorization: Bearer YOUR_TOKEN" \
  https://planer.m-k-mendykhan.workers.dev/api/profile
```

## 🐛 Возможные проблемы

### Письмо не приходит?

1. Проверьте папку "Спам"
2. Используйте кнопку "Resend Verification Email"
3. Убедитесь в правильности email

### Ошибка "Email not verified"?

- Вы пытаетесь войти без подтверждения email
- Проверьте почту и кликните на ссылку

### Ошибка с KV namespace?

- Убедитесь, что вы создали namespace
- Проверьте ID в `wrangler.jsonc`
- Попробуйте удалить и создать заново

## 📚 Подробная документация

Для более детальной информации смотрите [AUTH_SETUP.md](./AUTH_SETUP.md)

## 🎯 Следующие шаги

После настройки авторизации вы можете:

1. Добавить защиту к своим API endpoints через `authMiddleware`
2. Использовать `c.get('user')` для получения данных пользователя
3. Создать роли и разрешения
4. Добавить социальную авторизацию (Google, Facebook и т.д.)
5. Настроить Multi-factor Authentication (MFA)

---

**Важно:** Не забудьте заменить `YOUR_KV_ID_HERE` в `wrangler.jsonc` перед деплоем! 🚨

