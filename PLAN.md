# План реализации недостающего функционала

## ✅ Что уже реализовано

### 1. Пользовательские задачи (Tasks)
- ✅ Создание задачи (все поля: название, описание, дата, дедлайн, приоритет, теги, статус)
- ✅ Просмотр задач (список с фильтрами, календарь)
- ✅ Редактирование задачи
- ✅ Удаление задачи (мягкое - архив, жесткое - удаление)
- ✅ Поиск по названию
- ✅ Фильтры: статус, приоритет, период, поиск
- ✅ Дублирование задачи

### 2. Приоритеты и теги
- ✅ Все приоритеты (низкий, средний, высокий, критический)
- ✅ Создание пользовательских тегов
- ✅ Цветные метки для тегов
- ✅ Группировка задач по тегам

### 3. UI-функциональность (частично)
- ✅ Лента задач на сегодня (Dashboard)
- ✅ Быстрое добавление задачи (FAB)
- ✅ Календарь с задачами

---

## ❌ Что НЕ реализовано

### 1. Периодические задачи (Recurrence) - **КРИТИЧНО**

#### 1.1. Типы повторений
- ❌ UI для настройки правил повторения в TaskForm
- ❌ Сервис для работы с recurrence rules (RecurrenceService)
- ❌ API endpoints для CRUD операций с recurrence rules
- ❌ Логика генерации инстансов задач по расписанию
- ❌ Cron job / Scheduled task для автоматической генерации инстансов

#### 1.2. Правила остановки повторений
- ❌ UI для настройки end_type (never/date/count)
- ❌ Валидация и проверка условий остановки

#### 1.3. Логика появления задач
- ❌ Таблица `task_instances` в БД (если нужна)
- ❌ Генерация инстансов при создании периодической задачи
- ❌ Автоматическая генерация будущих инстансов
- ❌ Возможность редактировать отдельный инстанс без влияния на серию
- ❌ Отметка инстансов как выполненных
- ❌ Перенос инстансов
- ❌ Удаление отдельных инстансов

### 2. Просмотр задач - **СРЕДНИЙ ПРИОРИТЕТ**

#### 2.1. Представления
- ❌ Переключатель "Сегодня / Неделя / Месяц" на главном экране
- ❌ Список задач на неделю (отдельное представление)
- ❌ Список задач на месяц (отдельное представление)
- ❌ Группировка задач по неделям/месяцам

#### 2.2. Фильтры
- ✅ Фильтр по тегам (есть в типах, нужно проверить UI)
- ✅ Фильтр по периоду (реализован через start_date/end_date)

### 3. Детальный просмотр задачи - **ВЫСОКИЙ ПРИОРИТЕТ**

#### 3.1. Компонент TaskDetail
- ❌ Страница детального просмотра задачи (`/tasks/:id`)
- ❌ Полная информация о задаче
- ❌ История изменений (TaskHistory)
- ❌ Управление периодичностью (для периодических задач)
- ❌ Дублирование задачи (есть в меню, но нужно проверить)
- ❌ Редактирование из детального просмотра

#### 3.2. История изменений
- ❌ Сервис для работы с историей (TaskHistoryService)
- ❌ API endpoints для получения истории задачи
- ❌ Логирование изменений при обновлении задачи
- ❌ UI для отображения истории изменений

### 4. UI-функциональность - **СРЕДНИЙ ПРИОРИТЕТ**

#### 4.1. Главный экран
- ✅ Лента задач на сегодня
- ✅ Быстрое добавление задачи
- ❌ Переключатель "Сегодня / Неделя / Месяц"

#### 4.2. Экран задачи
- ❌ Полная информация (нужен TaskDetail)
- ❌ История изменений
- ❌ Управление периодичностью
- ✅ Дублирование задачи (реализовано)

---

## 📋 План реализации (по приоритетам)

### Фаза 1: Критичный функционал (1-2 недели)

#### 1.1. Детальный просмотр задачи
**Приоритет: ВЫСОКИЙ**
- [ ] Создать компонент `TaskDetail.tsx`
- [ ] Добавить API метод `getTaskHistory(taskId)`
- [ ] Создать `TaskHistoryService` на бэкенде
- [ ] Реализовать логирование изменений в `TaskService.updateTask()`
- [ ] Отобразить полную информацию о задаче
- [ ] Отобразить историю изменений
- [ ] Добавить кнопки действий (редактировать, дублировать, удалить)

#### 1.2. Периодические задачи - базовая функциональность
**Приоритет: ВЫСОКИЙ**
- [ ] Создать `RecurrenceService` на бэкенде
- [ ] Добавить API endpoints для recurrence rules:
  - `POST /api/tasks/:id/recurrence` - создать правило
  - `GET /api/tasks/:id/recurrence` - получить правило
  - `PUT /api/tasks/:id/recurrence` - обновить правило
  - `DELETE /api/tasks/:id/recurrence` - удалить правило
- [ ] Создать компонент `RecurrenceRuleForm.tsx` для настройки правил
- [ ] Интегрировать форму в `TaskForm.tsx`
- [ ] Реализовать базовую логику генерации инстансов (ежедневно, еженедельно)

### Фаза 2: Важный функционал (2-3 недели)

#### 2.1. Периодические задачи - полная реализация
**Приоритет: СРЕДНИЙ**
- [ ] Реализовать все типы повторений:
  - Ежедневно
  - Еженедельно (с выбором дней недели)
  - Ежемесячно (по дню месяца или по дню недели)
  - Ежегодно
  - Кастомное (каждые N часов/дней/недель/месяцев)
  - По рабочим дням (пн-пт)
  - По выходным
- [ ] Реализовать правила остановки:
  - Никогда (бессрочно)
  - До определённой даты
  - До количества повторений
- [ ] Создать scheduled task (cron) для генерации инстансов
- [ ] Реализовать работу с инстансами:
  - Генерация инстансов при создании периодической задачи
  - Автоматическая генерация будущих инстансов
  - Редактирование отдельного инстанса
  - Удаление отдельного инстанса

#### 2.2. Улучшение просмотра задач
**Приоритет: СРЕДНИЙ**
- [ ] Добавить переключатель представлений на Dashboard:
  - Сегодня
  - Неделя
  - Месяц
- [ ] Создать компонент `WeekView.tsx` для недельного представления
- [ ] Создать компонент `MonthView.tsx` для месячного представления
- [ ] Реализовать группировку задач по неделям/месяцам

### Фаза 3: Дополнительный функционал (1-2 недели)

#### 3.1. Улучшение UI
**Приоритет: НИЗКИЙ**
- [ ] Добавить фильтр по тегам в UI (если не реализован)
- [ ] Улучшить отображение периодических задач (иконка повторения)
- [ ] Добавить визуальное отображение связи между инстансами и родительской задачей
- [ ] Улучшить календарь (подсветка дней с задачами, быстрый переход)

#### 3.2. Оптимизация и доработки
**Приоритет: НИЗКИЙ**
- [ ] Оптимизация запросов для больших объемов данных
- [ ] Пагинация для списков задач
- [ ] Кэширование данных
- [ ] Обработка edge cases для периодических задач

---

## 🔧 Технические детали

### База данных

**Нужно проверить/создать таблицы:**
- `recurrence_rules` - правила повторения
- `task_instances` - инстансы периодических задач (опционально, если нужна отдельная таблица)
- `task_history` - история изменений задач

**Структура `recurrence_rules`:**
```sql
CREATE TABLE recurrence_rules (
  id TEXT PRIMARY KEY,
  task_id TEXT NOT NULL,
  type TEXT NOT NULL, -- 'daily', 'weekly', 'monthly', 'yearly', 'custom', 'workdays', 'weekends'
  interval INTEGER NOT NULL DEFAULT 1,
  days_of_week TEXT, -- JSON array: [1,2,3] для пн,вт,ср
  day_of_month INTEGER,
  end_type TEXT NOT NULL, -- 'never', 'date', 'count'
  end_date TEXT,
  end_count INTEGER,
  created_at TEXT NOT NULL,
  FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE
);
```

**Структура `task_history`:**
```sql
CREATE TABLE task_history (
  id TEXT PRIMARY KEY,
  task_id TEXT NOT NULL,
  user_id TEXT NOT NULL,
  field_name TEXT NOT NULL,
  old_value TEXT,
  new_value TEXT,
  changed_at TEXT NOT NULL,
  FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### API Endpoints

**Новые endpoints для recurrence:**
- `POST /api/tasks/:id/recurrence` - создать правило повторения
- `GET /api/tasks/:id/recurrence` - получить правило
- `PUT /api/tasks/:id/recurrence` - обновить правило
- `DELETE /api/tasks/:id/recurrence` - удалить правило

**Новые endpoints для history:**
- `GET /api/tasks/:id/history` - получить историю изменений

**Новые endpoints для instances (если нужны):**
- `GET /api/tasks/:id/instances` - получить все инстансы задачи
- `PATCH /api/tasks/:id/instances/:instanceId` - обновить инстанс
- `DELETE /api/tasks/:id/instances/:instanceId` - удалить инстанс

### Scheduled Tasks

**Для генерации инстансов:**
- Использовать Cloudflare Workers Cron Triggers
- Запускать ежедневно в определенное время
- Генерировать инстансы на следующий период (например, на месяц вперед)

---

## 📝 Примечания

1. **Периодические задачи** - самый сложный функционал, требует тщательной проработки логики генерации инстансов
2. **История изменений** - нужно решить, какие поля логировать (все или только важные)
3. **Инстансы задач** - можно реализовать двумя способами:
   - Отдельная таблица `task_instances` (более гибко, но сложнее)
   - Генерация обычных задач с ссылкой на родительскую (проще, но менее гибко)
4. **Производительность** - при большом количестве периодических задач нужно оптимизировать генерацию инстансов








