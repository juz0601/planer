# Firebase Authentication Setup Guide

## Обзор

В проект интегрирована полная система авторизации через Firebase Authentication с использованием email/password и обязательной проверкой email.

## Реализованные функции

✅ Регистрация пользователей с email/password  
✅ Вход в систему  
✅ Сброс пароля через email  
✅ Обязательная проверка email перед доступом к приложению  
✅ Автоматическая отправка письма с подтверждением после регистрации  
✅ Перенаправление на приложение после подтверждения email  
✅ JWT токены в localStorage  
✅ Защита API endpoints на backend  
✅ Автоматическая проверка токенов с помощью middleware  
✅ Красивый UI для логина/регистрации/сброса пароля с Material-UI  

## Архитектура

### Backend (Cloudflare Workers)

- **`worker/middleware/auth.ts`** - Middleware для проверки Firebase токенов
- **`worker/index.ts`** - Настроенные публичные и защищенные маршруты
- **`worker/types.ts`** - Типы для Firebase Auth и Hono контекста
- **`wrangler.jsonc`** - Конфигурация с KV namespace для кэширования публичных ключей

### Frontend (React + MUI)

- **`src/config/firebase.ts`** - Конфигурация Firebase
- **`src/contexts/AuthContext.tsx`** - Context для управления состоянием авторизации
- **`src/components/Login.tsx`** - Компонент входа
- **`src/components/Register.tsx`** - Компонент регистрации
- **`src/components/PasswordReset.tsx`** - Компонент сброса пароля
- **`src/App.tsx`** - Главный компонент с защитой маршрутов

## Настройка

### Шаг 1: Создание KV Namespace

Перед деплоем необходимо создать KV namespace для кэширования публичных ключей Firebase:

```bash
# Создать KV namespace
npx wrangler kv:namespace create PUBLIC_JWK_CACHE_KV

# Для production окружения
npx wrangler kv:namespace create PUBLIC_JWK_CACHE_KV --preview false
```

После создания вы получите ID вида:

```
🌀 Creating namespace with title "planer-PUBLIC_JWK_CACHE_KV"
✨ Success!
Add the following to your wrangler.jsonc:
{ binding = "PUBLIC_JWK_CACHE_KV", id = "YOUR_KV_ID_HERE" }
```

### Шаг 2: Обновление wrangler.jsonc

Замените `YOUR_KV_ID_HERE` в `wrangler.jsonc` на полученный ID:

```json
"kv_namespaces": [
  {
    "binding": "PUBLIC_JWK_CACHE_KV",
    "id": "YOUR_ACTUAL_KV_ID"
  }
]
```

### Шаг 3: Настройка Firebase Console

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект **planer-246d3**
3. Перейдите в **Authentication** → **Settings** → **Authorized domains**
4. Убедитесь, что добавлен домен: `planer.m-k-mendykhan.workers.dev`

5. В разделе **Authentication** → **Sign-in method**:
   - Включите **Email/Password** провайдер
   - Включите **Email link (passwordless sign-in)** если нужно

6. Настройте шаблон письма для подтверждения email:
   - Перейдите в **Authentication** → **Templates** → **Email address verification**
   - Убедитесь, что в ссылке используется правильный домен: `https://planer.m-k-mendykhan.workers.dev/`

### Шаг 4: Переменные окружения (опционально)

Добавьте в `wrangler.jsonc` если нужны дополнительные настройки:

```json
"vars": {
  "PUBLIC_JWK_CACHE_KEY": "firebase_jwks"
}
```

## Деплой

```bash
# Build и deploy
npm run deploy
```

## Использование

### Frontend

#### Регистрация нового пользователя

```typescript
import { useAuth } from './contexts/AuthContext';

function RegisterComponent() {
  const { signUp } = useAuth();
  
  const handleRegister = async (email: string, password: string) => {
    try {
      await signUp(email, password);
      // Пользователь будет автоматически разлогинен
      // и получит письмо с подтверждением
    } catch (error) {
      console.error('Registration error:', error);
    }
  };
}
```

#### Вход пользователя

```typescript
import { useAuth } from './contexts/AuthContext';

function LoginComponent() {
  const { signIn } = useAuth();
  
  const handleLogin = async (email: string, password: string) => {
    try {
      await signIn(email, password);
      // Проверяется, что email подтвержден
      // Если нет - выбрасывается ошибка
    } catch (error) {
      console.error('Login error:', error);
    }
  };
}
```

#### Получение JWT токена для API запросов

```typescript
import { useAuth } from './contexts/AuthContext';

function ApiComponent() {
  const { getIdToken } = useAuth();
  
  const callProtectedApi = async () => {
    const token = await getIdToken();
    
    const response = await fetch('/api/profile', {
      headers: {
        'Authorization': `Bearer ${token}`,
      },
    });
  };
}
```

### Backend

#### Защищенные маршруты

```typescript
import { authMiddleware } from './middleware';

// Применить middleware ко всем маршрутам
protectedApi.use('/*', authMiddleware);

// Использовать user в обработчике
protectedApi.get('/profile', async (c) => {
  const user = c.get('user');
  return c.json({ user });
});
```

#### Опциональная авторизация

```typescript
import { optionalAuthMiddleware } from './middleware';

// Маршрут доступен всем, но user будет установлен если есть токен
publicApi.use('/*', optionalAuthMiddleware);

publicApi.get('/data', async (c) => {
  const user = c.get('user'); // undefined если не авторизован
  
  if (user) {
    // Возвращаем персонализированные данные
  } else {
    // Возвращаем публичные данные
  }
});
```

## Потоки работы (Workflows)

### 1. Регистрация нового пользователя

1. Пользователь заполняет форму регистрации (email + password)
2. Frontend вызывает `signUp(email, password)`
3. Firebase создает аккаунт
4. Автоматически отправляется письмо с подтверждением
5. Пользователь выходит из системы
6. Показывается экран "Check Your Email"

### 2. Подтверждение email

1. Пользователь получает письмо
2. Кликает на ссылку подтверждения
3. Firebase подтверждает email
4. Пользователь перенаправляется на `https://planer.m-k-mendykhan.workers.dev/`
5. Пользователь может войти в систему

### 3. Вход в систему

1. Пользователь заполняет форму входа
2. Frontend вызывает `signIn(email, password)`
3. Проверяется `emailVerified`
4. Если email не подтвержден - показывается ошибка
5. Если подтвержден - пользователь получает доступ к приложению
6. JWT токен кэшируется в localStorage

### 4. Сброс пароля

1. Пользователь на странице логина нажимает "Forgot password?"
2. Открывается форма сброса пароля
3. Пользователь вводит email
4. Frontend вызывает `resetPassword(email)`
5. Firebase отправляет письмо со ссылкой для сброса
6. Пользователь кликает на ссылку в письме
7. Открывается страница Firebase для установки нового пароля
8. Пользователь устанавливает новый пароль
9. Пользователь возвращается в приложение и входит с новым паролем

### 5. Доступ к защищенным API

1. Frontend получает токен через `getIdToken()`
2. Токен отправляется в заголовке `Authorization: Bearer <token>`
3. Backend middleware проверяет токен
4. Проверяется `email_verified` в токене
5. Если все ок - запрос проходит дальше
6. Если нет - возвращается 401/403

## Безопасность

### Backend

- ✅ Все JWT токены проверяются через Firebase Admin SDK
- ✅ Публичные ключи кэшируются в KV для производительности
- ✅ Проверяется `email_verified` на уровне middleware
- ✅ CORS настроен для защиты от XSS
- ✅ Токены имеют ограниченный срок действия

### Frontend

- ✅ Токены хранятся в localStorage (не в cookies для избежания CSRF)
- ✅ Автоматическое обновление токенов при необходимости
- ✅ Проверка состояния авторизации при загрузке приложения
- ✅ Автоматический редирект на логин если токен истек

## Тестирование

### Публичные endpoints (без авторизации)

```bash
# Test endpoint
curl https://planer.m-k-mendykhan.workers.dev/api/test

# Versions endpoint
curl https://planer.m-k-mendykhan.workers.dev/api/versions
```

### Защищенные endpoints (требуется токен)

```bash
# Получить токен из localStorage в браузере
# localStorage.getItem('firebaseToken')

# Profile endpoint
curl -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  https://planer.m-k-mendykhan.workers.dev/api/profile
```

## Troubleshooting

### "Email not verified" при входе

**Проблема**: Пользователь не может войти даже после регистрации  
**Решение**: Проверьте email и кликните на ссылку подтверждения

### "Invalid or expired token" на backend

**Проблема**: API возвращает 401 ошибку  
**Решение**: 
1. Проверьте, что токен актуален (не истек)
2. Попробуйте выйти и войти заново
3. Проверьте правильность `projectId` в конфигурации

### Письмо с подтверждением не приходит

**Проблема**: Пользователь не получает письмо  
**Решение**:
1. Проверьте папку "Спам"
2. Убедитесь, что email правильный
3. Используйте кнопку "Resend Verification Email"
4. Проверьте квоты в Firebase Console

### Письмо для сброса пароля не приходит

**Проблема**: После запроса сброса пароля письмо не приходит  
**Решение**:
1. Проверьте папку "Спам"
2. Убедитесь, что email правильный (используется существующий аккаунт)
3. Попробуйте отправить запрос еще раз
4. Проверьте шаблоны писем в Firebase Console → Authentication → Templates
5. Убедитесь, что в Firebase Console включен Email/Password провайдер

### KV namespace ошибка при деплое

**Проблема**: Ошибка связанная с `PUBLIC_JWK_CACHE_KV`  
**Решение**:
1. Убедитесь, что KV namespace создан
2. Проверьте правильность ID в `wrangler.jsonc`
3. Проверьте права доступа в Cloudflare Dashboard

## Дополнительные возможности

### Сброс пароля ✅

Функционал сброса пароля полностью реализован:

- Компонент `PasswordReset` с красивым UI
- Ссылка "Forgot password?" на странице логина
- Автоматическая отправка письма со ссылкой для сброса
- Перенаправление после установки нового пароля

Использование:

```typescript
import { useAuth } from './contexts/AuthContext';

function ResetPasswordComponent() {
  const { resetPassword } = useAuth();
  
  const handleReset = async (email: string) => {
    await resetPassword(email);
    // Пользователь получит письмо со ссылкой
  };
}
```

### Social Auth (Google, Facebook и т.д.)

Firebase поддерживает авторизацию через социальные сети. Можно легко добавить в будущем.

### Multi-factor Authentication (MFA)

Firebase поддерживает 2FA для дополнительной безопасности.

## Ссылки

- [Firebase Authentication Docs](https://firebase.google.com/docs/auth)
- [@hono/firebase-auth Docs](https://github.com/honojs/middleware/tree/main/packages/firebase-auth)
- [Cloudflare Workers KV](https://developers.cloudflare.com/kv/)
- [Material-UI Components](https://mui.com/material-ui/getting-started/)

## Контакты

При возникновении проблем или вопросов обращайтесь к документации или создайте issue в репозитории проекта.

